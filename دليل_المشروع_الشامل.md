# دليل مشروع "ديوني" الشامل — من الصفر حتى آخر سطر كود

هذا الملف مرجع تعليمي كامل لفهم بنية المشروع بالكامل: لماذا كل مجلد موجود، ماذا يفعل كل ملف،
ولماذا كُتب بهذا الشكل تحديدًا. اقرأه بالترتيب — كل قسم يبني على الذي قبله.

---

## الفهرس

1. [الفكرة الأساسية: لماذا "Hybrid Architecture"؟](#1)
2. [شجرة المشروع الكاملة](#2)
3. [شرح كل مجلد رئيسي ووظيفته](#3)
4. [ملفات الجذر (خارج `lib/`)](#4)
5. [لماذا مجلد `.github/` ولماذا CI؟](#5)
6. [لماذا مجلد `test/` مقسّم هكذا؟](#6)
7. [شرح تفصيلي — كل ملفات الترحيب (Onboarding) سطرًا سطرًا](#7)
8. [شرح باقي الشاشات والملفات](#8)
9. [خلاصة: كيف تتحرك البيانات من الضغطة إلى الشاشة](#9)

---

<a name="1"></a>
## 1) الفكرة الأساسية: لماذا "Hybrid Architecture"؟

في بداية المشروع كان أمامنا 3 خيارات لتنظيم الكود:

| الخيار | الفكرة |
|---|---|
| Feature-first | كل ميزة (مثل الترحيب، تسجيل الدخول) لها مجلد واحد يحوي كل شيء يخصها |
| Layer-first | كل الشاشات في مجلد، كل الـ Controllers في مجلد آخر، بغض النظر عن الميزة |
| **Hybrid (المُعتمد)** | مزيج: كل ميزة لها مساحتها الخاصة، **لكن** أي شيء يُستخدم من أكثر من ميزة (الألوان، الخطوط، معالجة الأخطاء، لاحقًا محرك الصوت) يُوضع في `core/` مشترك |

اخترنا Hybrid لأن مشروعك يحتوي على "زر صوتي عائم" سيظهر في كل شاشة تقريبًا لاحقًا — هذا ليس
ميزة تخص شاشة واحدة، بل بنية تحتية يحتاجها التطبيق كله. لو استخدمنا Feature-first البحت، كنا
سنُضطر لتكرار كود محرك الصوت داخل كل ميزة. Hybrid يحل هذا بأن يعيش محرك الصوت في `core/voice/`
(سيُضاف في المرحلة 3) بينما تبقى كل شاشة (ترحيب، دخول، تسجيل) في مساحتها الخاصة تحت `view/`.

بالإضافة لذلك، طبقنا **فصل الطبقات الأربع** داخل كل ميزة (هذا مبدأ منفصل عن Hybrid ويُطبَّق
معه):

```
شاشة (view) ──يطلب من──▶ منطق (logic) ──يطلب من──▶ مستودع (repository) ──يطلب من──▶ مصدر بيانات (data source)
```

كل طبقة **لا تعرف** كيف تعمل الطبقة التي تحتها بالتفصيل، فقط تعرف "الواجهة" (interface) الخاصة
بها. لماذا هذا مهم؟ لأنه يعني أنه يمكنك لاحقًا استبدال SharedPreferences بقاعدة بيانات حقيقية،
أو استبدال Firebase بخادم مختلف، **دون تعديل شاشة واحدة من شاشات `view/`**. هذا سيتضح عمليًا في
قسم شرح ملفات الترحيب أدناه.

---

<a name="2"></a>
## 2) شجرة المشروع الكاملة

```
duyoni/
├── pubspec.yaml                  # قائمة الحزم (المكتبات) التي يعتمد عليها المشروع
├── l10n.yaml                     # إعدادات توليد ملفات الترجمة
├── analysis_options.yaml         # قواعد فحص جودة الكود
├── .gitignore                    # الملفات التي لا يجب رفعها لـ Git
├── README.md                     # التوثيق العام للمشروع (منفصل عن هذا الدليل)
│
├── .github/
│   └── workflows/
│       └── ci.yml                # يشغّل الفحص والاختبارات تلقائيًا عند كل رفعة كود
│
├── assets/
│   ├── fonts/                    # ملفات الخطوط (فارغ حاليًا، ننتظرك تضيفها)
│   ├── icons/                    # شعار التطبيق وأيقونات ثابتة
│   └── images/                   # الرسومات التوضيحية (فارغ حاليًا)
│
├── lib/
│   ├── main.dart                 # نقطة بداية تشغيل التطبيق بالكامل
│   │
│   ├── core/                     # كل ما هو "بنية تحتية" مشتركة بين كل الميزات
│   │   ├── theme/                # الألوان والخطوط والثيم العام
│   │   ├── l10n/                 # ملفات الترجمة (عربي/إنجليزي)
│   │   ├── routes/               # تعريف مسارات التنقل بين الشاشات
│   │   └── utils/                # أدوات عامة (معالجة الأخطاء، نمط Result)
│   │
│   ├── data/                     # طبقة البيانات: من أين تُجلب المعلومات وكيف تُحفظ
│   │   ├── local/                # مصادر بيانات محلية (تخزين على الجهاز)
│   │   │   └── onboarding/
│   │   └── repositories/         # المستودعات — الواجهة التي يتعامل معها بقية التطبيق
│   │       ├── onboarding/
│   │       └── auth/
│   │
│   ├── logic/                    # طبقة المنطق: القرارات والحالة (Riverpod)
│   │   ├── onboarding/
│   │   └── auth/
│   │
│   └── view/                     # كل ما يُرسم على الشاشة
│       ├── screens/              # الشاشات الكاملة (كل شاشة = ملف واحد رئيسي)
│       │   ├── splash/
│       │   ├── onboarding/
│       │   ├── auth/
│       │   └── home/
│       └── widgets/              # قطع واجهة صغيرة قابلة لإعادة الاستخدام
│           ├── shared/           # تُستخدم في أكثر من ميزة
│           ├── onboarding/       # خاصة بشاشة الترحيب فقط
│           └── auth/             # خاصة بشاشتي الدخول/التسجيل فقط
│
└── test/                         # كل الاختبارات الآلية
    ├── unit/                     # اختبارات المنطق بدون واجهة
    │   └── onboarding/
    └── widget/                   # اختبارات الواجهة كما يراها المستخدم
        ├── onboarding/
        └── auth/
```

---

<a name="3"></a>
## 3) شرح كل مجلد رئيسي ووظيفته

### `lib/core/` — البنية التحتية المشتركة
كل شيء هنا **لا يخص ميزة واحدة**، بل يُستخدم من كل الشاشات. القاعدة الذهبية: لو حذفت ميزة كاملة
(مثلاً الترحيب)، يجب ألا يتأثر أي ملف داخل `core/`.

- **`core/theme/`**: مصدر واحد لكل لون وكل خط في التطبيق. لماذا؟ لأنك لو أردت غدًا تغيير اللون
  الأساسي من أخضر إلى أي لون آخر، تُعدّل سطرًا واحدًا في `app_colors.dart` بدل البحث في عشرات
  الملفات عن كل مكان استُخدم فيه اللون يدويًا.
- **`core/l10n/`**: ملفات الترجمة. أي نص يظهر للمستخدم يُكتب هنا مرة واحدة بالعربي والإنجليزي،
  والشاشات تستدعيه بالاسم فقط. هذا يمنع وجود نص عربي "مطبوع" مباشرة داخل كود الشاشة.
- **`core/routes/`**: خريطة التنقل. ملف واحد يعرف "عند الذهاب لـ `/login` اعرض أي شاشة" — بدل أن
  تكتب كل شاشة منطق التنقل الخاص بها بشكل متفرق.
- **`core/utils/`**: أدوات عامة لا تخص ميزة معينة، أهمها نظام معالجة الأخطاء (`AppException`)
  ونمط النتيجة (`Result`) المشروحان بالتفصيل في قسم 7.

### `lib/data/` — من أين تأتي البيانات؟
هذه الطبقة **الوحيدة** المسموح لها بالتحدث مباشرة مع مصادر خارجية: قاعدة بيانات محلية،
SharedPreferences، أو خوادم Firebase. مقسّمة لطبقتين فرعيتين:

- **`data/local/`**: يتحدث مباشرة مع Flutter/الجهاز (مثل SharedPreferences). لا يعرف أي "قواعد
  عمل" — فقط "احفظ هذه القيمة" أو "أعطني هذه القيمة".
- **`data/repositories/`**: الواجهة الرسمية التي تتعامل معها بقية التطبيق. كل ميزة لها Repository
  خاص بها (واجهة `abstract class` + تنفيذ `Impl`). لماذا هذا الفصل بين الواجهة والتنفيذ؟ لأن
  الاختبارات (`test/`) تستطيع استبدال التنفيذ الحقيقي بتنفيذ وهمي (Mock) دون الحاجة لتشغيل
  SharedPreferences أو Firebase الحقيقيين أثناء الاختبار — سترى هذا عمليًا في قسم الاختبارات.

### `lib/logic/` — القرارات والحالة
هنا تعيش كل "الحالة" (State) التي تتغير أثناء استخدام التطبيق: هل انتهى المستخدم من الترحيب؟ هل
تسجيل الدخول قيد التنفيذ الآن؟ نستخدم **Riverpod** لإدارة هذه الحالة (تم اختياره في بداية المشروع
لأن المشروع كبير، جديد بالكامل، ويحتاج اختبار منطق معقد لاحقًا مثل حوار المساعد الصوتي).
`logic/` يستدعي `data/repositories/` فقط — لا يعرف تفاصيل SharedPreferences أو Firebase إطلاقًا.

### `lib/view/` — كل ما يُرى على الشاشة
مقسّم لقسمين:
- **`view/screens/`**: الشاشة الكاملة كما يراها المستخدم (شاشة الترحيب، شاشة الدخول...).
- **`view/widgets/`**: قطع صغيرة قابلة لإعادة الاستخدام، ومقسّمة بدورها إلى:
  - `widgets/shared/`: تُستخدم في أكثر من شاشة (مثل الزر الأساسي).
  - `widgets/onboarding/` و`widgets/auth/`: خاصة بميزة واحدة فقط، لا معنى لاستخدامها في مكان آخر.

### `assets/`
ملفات ثابتة (خطوط، صور، أيقونات) لا تُكتب بالكود بل تُحمَّل منها. مقسّمة حسب النوع
(`fonts/`, `icons/`, `images/`) لأن `pubspec.yaml` يحتاج تسجيل كل مجلد أصول بشكل صريح.


---

<a name="4"></a>
## 4) ملفات الجذر (خارج `lib/`)

### `pubspec.yaml`
هذا "بطاقة هوية" المشروع لأداة Flutter. يحتوي على:
- **`dependencies`**: كل حزمة (مكتبة جاهزة) يحتاجها التطبيق ليعمل فعليًا (مثل `flutter_riverpod`
  لإدارة الحالة، `firebase_auth` للمصادقة). لاحظ أن حزم Firebase مثبّتة برقم إصدار **دقيق**
  (مثلاً `5.3.1` وليس `^5.3.1`) لأنها حساسة أمنيًا — أي تحديث لها يجب أن يكون قرارًا واعيًا لا
  تحديثًا تلقائيًا صامتًا.
- **`dev_dependencies`**: حزم تُستخدم فقط أثناء التطوير والاختبار (مثل `mocktail` لصنع كائنات
  وهمية في الاختبارات)، ولا تدخل ضمن التطبيق النهائي الذي يثبته المستخدم.
- **`flutter: assets:`**: يخبر Flutter "هذه المجلدات تحتوي ملفات يجب تضمينها عند بناء التطبيق".
- **`flutter: fonts:`**: معلّق حاليًا (بـ `#`) لأن ملفات الخطوط الحقيقية لم تُضَف بعد — بمجرد أن
  تضعها في `assets/fonts/` تُفعّل هذا القسم بإزالة التعليق.

### `l10n.yaml`
ملف صغير يخبر أداة `flutter gen-l10n`: "ملفات الترجمة موجودة في `lib/core/l10n/`، والملف
الأساسي (القالب) هو `app_ar.arb`، وأنشئ لي ملف Dart باسم `app_localizations.dart` يحتوي على دالة
لكل نص مترجم". هذا الملف المُولَّد هو ما يسمح لك بكتابة `l10n.loginTitle` في الكود بدل كتابة
النص العربي مباشرة.

### `analysis_options.yaml`
قواعد صارمة لفحص الكود قبل حتى تشغيله، مثل:
- `strict-casts`, `strict-inference`, `strict-raw-types`: تمنع أخطاء الأنواع الخفية.
- `avoid_print`: تمنع استخدام `print()` للتصحيح (يجب استخدام أداة تسجيل حقيقية بدلها).
- `close_sinks`, `cancel_subscriptions`: تكتشف تسريبات الذاكرة (مثل نسيان إغلاق Stream).
- `require_trailing_commas`: تفرض فاصلة بعد آخر عنصر في القوائم — هذا يجعل الفروقات في Git
  (diffs) أنظف عند إضافة عنصر جديد لاحقًا.

هذا الملف هو ما يجعل أمر `flutter analyze` (المستخدم في CI، انظر القسم التالي) يفشل إذا كتب أي
مطور كودًا لا يتبع هذه المعايير — حماية جودة تلقائية بدل الاعتماد على المراجعة اليدوية فقط.

### `.gitignore`
يمنع رفع ملفات لا يجب أن تصل لـ Git أبدًا:
- `.dart_tool/`, `build/`: ملفات مؤقتة يعيد Flutter توليدها تلقائيًا، لا فائدة من حفظها.
- `*.g.dart`, `*.freezed.dart`: ملفات Dart مولَّدة تلقائيًا من أدوات أخرى (لم نستخدمها بعد لكن
  المكان محجوز لها مستقبلًا).
- `*.jks`, `*.keystore`, `key.properties`: **الأهم أمنيًا** — ملفات توقيع تطبيق أندرويد. لو
  رُفعت هذه لمستودع عام، يستطيع أي شخص توقيع نسخة مزيفة من تطبيقك تبدو "رسمية".

---

<a name="5"></a>
## 5) لماذا مجلد `.github/` ولماذا CI؟

`.github/workflows/ci.yml` هو ملف يفهمه موقع GitHub تحديدًا (لو رفعت المشروع هناك). "CI" تعني
Continuous Integration — **تكامل مستمر**: بدل أن تعتمد على تذكّرك الشخصي لتشغيل الاختبارات قبل كل
رفعة كود، يشغّلها GitHub تلقائيًا لك في كل مرة.

محتوى الملف بالتفصيل:

```yaml
on: [push, pull_request]
```
يعني: "شغّل هذا كلما رفع أحد كودًا جديدًا (push) أو فتح طلب دمج (pull request)".

```yaml
- run: flutter pub get       # يحمّل كل الحزم من pubspec.yaml
- run: dart format --output=none --set-exit-if-changed .   # يتأكد أن التنسيق موحّد
- run: flutter analyze        # يشغّل كل قواعد analysis_options.yaml
- run: flutter test           # يشغّل كل ملفات test/
```

**لماذا هذا مهم من اليوم الأول رغم أن المشروع في بدايته؟** لأنه أرخص بكثير أن تكتشف أن اختبارًا
تعطّل بعد دقيقتين من رفع الكود، بدل اكتشافه بعد أسبوعين حين تكون قد بنيت فوق كود معطّل بدون أن
تعرف. كما أنه يمنع نسيان ملف منسي التنسيق أو خطأ نوع بيانات بسيط كان يمكن اكتشافه فورًا.

**ماذا لو لم أستخدم GitHub؟** الملف غير ضار — ببساطة لن يعمل، والمشروع سيستمر بالعمل بشكل طبيعي
تمامًا. لكن يُنصح بالاحتفاظ به جاهزًا لأن أغلب الفرق تنتقل لاستخدام Git/GitHub في مرحلة ما.

---

<a name="6"></a>
## 6) لماذا مجلد `test/` مقسّم هكذا؟

```
test/
├── unit/       # اختبارات "منطق بحت" — بدون أي واجهة رسومية
└── widget/     # اختبارات "كيف تبدو وتتصرف الشاشة" كما يراها المستخدم
```

هذا التقسيم ليس تعسفيًا — كل نوع يختبر شيئًا مختلفًا تمامًا ويحتاج أدوات مختلفة:

### `test/unit/`
يختبر ملفات `logic/` بمعزل تام عن Flutter نفسه — لا يوجد فيه أي `Widget` أو `Scaffold` أو
شاشة. مثال: `onboarding_controller_test.dart` يتحقق أن الدالة `complete()` في
`OnboardingCompletedController` تستدعي `completeOnboarding()` من الـ Repository وتُحدّث
الحالة إلى `true` — **دون فتح أي شاشة فعلية**. هذا يجعل الاختبار سريعًا جدًا (أجزاء من الثانية)
لأنه لا يحتاج "رسم" أي شيء.

كيف يتم هذا بدون Repository حقيقي؟ عبر **Mock** (كائن وهمي):
```dart
class MockOnboardingRepository extends Mock implements OnboardingRepository {}
```
هذا يصنع نسخة مزيفة من الواجهة (interface) نفسها التي شرحناها في القسم 3 — نفس الفكرة التي
جعلت فصل الواجهة عن التنفيذ مفيدًا: الاختبار يستبدل `OnboardingRepositoryImpl` الحقيقي (الذي
يتحدث مع SharedPreferences فعليًا) بـ `MockOnboardingRepository` الذي "يتظاهر" بالنجاح أو
الفشل حسب ما نطلبه نحن في سطر الاختبار (`when(...).thenAnswer(...)`).

### `test/widget/`
يختبر الشاشة كما يتفاعل معها المستخدم فعليًا: هل يظهر النص الصحيح؟ هل الضغط على الزر ينقل
لشاشة أخرى؟ هذا يحتاج `tester.pumpWidget(...)` الذي "يرسم" الشاشة فعليًا داخل بيئة اختبار
مُحاكاة، ثم `tester.tap(...)` لمحاكاة ضغطة مستخدم حقيقية.

### لماذا مجلد فرعي لكل ميزة داخل `unit/` و`widget/` (مثل `onboarding/`, `auth/`)؟
نفس منطق تنظيم `lib/` بالضبط — بحيث يسهل عليك إيجاد اختبار ميزة معينة فورًا، وحتى لا يصبح
مجلد الاختبارات "بحرًا" من عشرات الملفات المتشابهة الاسم بلا تصنيف مع نمو المشروع.

**قاعدة عامة اعتمدناها من أول شاشة:** كل شاشة أو منطق جديد **يجب** أن يأتي معه اختبار في نفس
الرفعة، وليس "لاحقًا". هذا واضح في أن كل تسليم سابق لك تضمّن ملفات `test/` مقابلة لكل شاشة —
هذه ليست صدفة بل قاعدة عمل ثابتة منذ الشاشة الأولى.

---

<a name="7"></a>
## 7) شرح تفصيلي — كل ملفات الترحيب (Onboarding) سطرًا سطرًا

سنمشي بنفس ترتيب تدفق البيانات: من الأسفل (التخزين الفعلي) للأعلى (الشاشة التي تراها)، لأن هذا
هو الترتيب المنطقي لفهم "من أين يأتي كل شيء".

### 7.1) `lib/core/l10n/app_ar.arb` و `app_en.arb`

**لماذا هنا بالذات؟** لأنها ليست خاصة بميزة الترحيب فقط — ملف واحد يجمع نصوص كل الشاشات
(الترحيب، الدخول، التسجيل...)، لذلك مكانها الطبيعي `core/l10n/` لا `view/widgets/onboarding/`.

**الشكل العام:**
```json
{
  "@@locale": "ar",
  "onboardingWelcomeTitle": "مرحبًا بك في",
  "onboardingWelcomeSubtitlePrefix": "تسجيل الديون أصبح أسهل ",
  "onboardingWelcomeSubtitleHighlight": "بصوتك",
  ...
}
```
- `"@@locale": "ar"`: يخبر أداة الترجمة أن هذا الملف هو النسخة العربية. الملف المقابل
  `app_en.arb` له `"@@locale": "en"` بنفس المفاتيح تمامًا لكن بقيم إنجليزية.
- كل مفتاح (مثل `onboardingWelcomeTitle`) يتحول تلقائيًا عند تشغيل `flutter gen-l10n` إلى دالة
  Dart باسم `l10n.onboardingWelcomeTitle` تُرجع النص بلغة المستخدم الحالية.
- **لماذا قسّمنا نص الشريحة الأولى لثلاثة مفاتيح منفصلة** (`...Prefix`, `...Highlight`,
  `...Suffix`) بدل نص واحد؟ لأن التصميم المرجعي يُلوّن كلمة "بصوتك" بالذهبي وسط جملة عادية. لو
  كتبنا الجملة كنص واحد، لن نستطيع تلوين كلمة واحدة منه فقط. بتقسيمها لثلاث قطع، نبني في الشاشة
  ثلاث `TextSpan` منفصلة، كل واحدة بتنسيقها الخاص (سترى هذا في قسم 7.7).
- لاحظ أن **كل نص يظهر للمستخدم في شاشة الترحيب موجود هنا** ولا يوجد نص عربي واحد مكتوب مباشرة
  داخل ملفات `.dart` الخاصة بالترحيب — هذا مقصود ليعمل التبديل بين العربي والإنجليزي فورًا من
  نقطة واحدة.

### 7.2) `lib/core/theme/app_colors.dart`

**لماذا هنا؟** نفس السبب — الألوان مشتركة بين كل الشاشات، ليست حكرًا على الترحيب، رغم أننا
استخرجناها أصلًا من تحليل تصميم الترحيب.

**أهم ما يخص الترحيب تحديدًا من هذا الملف:**
```dart
static const primary = Color(0xFF163832);       // الأخضر الداكن — الأزرار والشارات والشعار
static const accent = Color(0xFFC9A24C);         // الذهبي — زر "ابدأ الآن" والكلمة المميزة
static const indicatorActive = Color(0xFF163832);   // نقطة المؤشر النشطة
static const indicatorInactive = Color(0xFFDCD9CE); // نقاط المؤشر غير النشطة
```
**لماذا `static const` تحديدًا؟** `static` تعني أن اللون ينتمي للـ Class نفسه لا لأي نسخة منه —
تكتب `AppColors.primary` مباشرة بدون إنشاء كائن. `const` تعني أن القيمة تُحسب مرة واحدة فقط وقت
البناء (compile time) لا تتكرر حسابها في كل مرة تُرسم فيها الشاشة — أداء أفضل قليلاً وضمان أن
القيمة لا تتغير أبدًا بالخطأ أثناء التشغيل.

### 7.3) `lib/core/theme/app_text_styles.dart` و `app_theme.dart`

نفس منطق `app_colors.dart` — مصدر واحد للخطوط والأحجام (`AppTextStyles.headline(context)`،
`AppTextStyles.body(context)`...) بدل تكرار `TextStyle(fontSize: 26, fontWeight: ...)` يدويًا في
كل شاشة. `app_theme.dart` يجمّع هذه الأنماط في `ThemeData` واحد (فاتح وداكن) يُمرَّر لـ
`MaterialApp` مرة واحدة في `main.dart` (قسم 7.11).

### 7.4) `lib/data/local/onboarding/onboarding_local_datasource.dart`

**لماذا هذا المسار بالذات (`data/local/onboarding/`)؟** لأن هذا الملف هو **الوحيد** في كل ميزة
الترحيب المسموح له بالتحدث مباشرة مع `SharedPreferences` (وهي طريقة Flutter لحفظ قيم صغيرة على
جهاز المستخدم، مثل "نعم/لا"، تبقى محفوظة حتى بعد إغلاق التطبيق).

```dart
import 'package:shared_preferences/shared_preferences.dart';
import '../../../core/utils/app_exception.dart';
```
السطر الأول يستورد حزمة SharedPreferences نفسها. السطر الثاني يستورد `StorageException` التي
سنستخدمها بدل رمي أخطاء عامة غير مفهومة.

```dart
/// Talks to local storage only. No business rules — the Repository decides what the flag means.
class OnboardingLocalDataSource {
```
هذا التعليق أعلى الـ Class **قاعدة تصميم مقصودة**: هذا الملف "غبي" عمدًا — لا يعرف *معنى* القيمة
التي يحفظها، فقط يحفظها ويعيدها. القرار بما تعنيه القيمة (مثل "المستخدم انتهى من الترحيب") يعيش
في طبقة أعلى (الـ Repository). هذا يسمح لاحقًا باستبدال SharedPreferences بأي تخزين آخر دون لمس
منطق العمل إطلاقًا.

```dart
  const OnboardingLocalDataSource(this._prefs);
  final SharedPreferences _prefs;
```
هذا **Constructor** (دالة إنشاء الكائن) يستقبل نسخة `SharedPreferences` جاهزة من الخارج بدل أن
يُنشئها بنفسه (`SharedPreferences.getInstance()` داخل الكلاس). هذا مبدأ اسمه **Dependency
Injection** (حقن الاعتماديات) — الفائدة العملية: في الاختبارات (قسم 6) نستطيع تمرير نسخة وهمية
بدل الحقيقية دون تعديل هذا الملف إطلاقًا. الشرطة السفلية `_prefs` تعني أن هذا المتغير **خاص**
(private) — لا يمكن لأي ملف آخر خارج هذا الملف الوصول إليه مباشرة، فقط عبر الدوال المعرَّفة هنا.

```dart
  static const _seenKey = 'onboarding_seen';
```
النص الحرفي (`'onboarding_seen'`) الذي سيُستخدم كـ "مفتاح" داخل SharedPreferences — نضعه في
متغير واحد بدل كتابته حرفيًا في مكانين (القراءة والكتابة)، فلو أخطأت لاحقًا بحرف واحد في أحد
المكانين، ستقرأ قيمة خاطئة دائمًا بصمت دون أي خطأ ظاهر — بمتغير واحد هذا الخطأ مستحيل.

```dart
  Future<bool> hasSeenOnboarding() async {
    try {
      return _prefs.getBool(_seenKey) ?? false;
    } catch (e) {
      throw StorageException(internalDetail: e.toString());
    }
  }
```
- `Future<bool>`: الدالة "غير متزامنة" (asynchronous) — تُرجع قيمة **لاحقًا** لا فورًا، لأن
  القراءة من تخزين الجهاز قد تأخذ جزءًا من الثانية. `async` تسمح باستخدام `await` بداخلها.
- `_prefs.getBool(_seenKey)`: يطلب القيمة المخزَّنة تحت هذا المفتاح. تُرجع `bool?` (إما `true`،
  `false`، أو `null` لو لم تُحفظ قيمة بعد أبدًا — أول تشغيل للتطبيق مثلًا).
- `?? false`: **عامل الدمج الفارغ** (null-coalescing) — "لو كانت القيمة `null`، استخدم `false`
  بدلها". هذا يعني: "لو لم يُحدَّد شيء بعد، افترض أن المستخدم لم يرَ الترحيب".
- `try/catch`: لو فشلت العملية لأي سبب (خطأ نظام، تخزين ممتلئ...)، لا نترك الخطأ الخام يتسرب
  لأعلى، بل نلفّه بـ `StorageException` المعرَّفة في `core/utils/app_exception.dart` — سبب هذا
  التصميم مشروح بالتفصيل في قسم 7.9 عند شرح ذلك الملف.

```dart
  Future<void> markOnboardingSeen() async {
    try {
      await _prefs.setBool(_seenKey, true);
    } catch (e) {
      throw StorageException(internalDetail: e.toString());
    }
  }
```
نفس المنطق بالضبط لكن للكتابة: `Future<void>` تعني أن الدالة لا تُرجع أي قيمة، فقط تُنفّذ فعلًا
(الحفظ) وتنتهي. `await` هنا **إلزامي** — بدونها، الكود سيكمل التنفيذ قبل انتهاء عملية الحفظ
الفعلية على القرص.

### 7.5) `lib/data/repositories/onboarding/onboarding_repository.dart`

```dart
/// `logic/` imports this interface only — never the DataSource directly (see repository-di.md).
abstract class OnboardingRepository {
  Future<bool> hasSeenOnboarding();
  Future<void> completeOnboarding();
}
```
هذا **ليس** كودًا يعمل فعليًا — إنه `abstract class` (واجهة/عقد). يقول: "أي شيء يريد أن يكون
`OnboardingRepository` يجب أن يوفّر هاتين الدالتين بالضبط، لكن لا يهمني *كيف* ينفذهما". لاحظ
الفرق البسيط لكن المهم في الاسم: `completeOnboarding()` هنا وليس `markOnboardingSeen()` كما في
الـ DataSource — لأن هذه الطبقة تتحدث بلغة **معنى العمل** ("أكمل المستخدم الترحيب") لا بلغة
التخزين الخام ("علّم قيمة كـ true"). هذا بالضبط ما قصدناه في تعليق 7.4 بأن الـ DataSource "غبي"
والـ Repository هو من يحمل المعنى.

### 7.6) `lib/data/repositories/onboarding/onboarding_repository_impl.dart`

```dart
import '../../local/onboarding/onboarding_local_datasource.dart';
import 'onboarding_repository.dart';

class OnboardingRepositoryImpl implements OnboardingRepository {
```
`implements OnboardingRepository`: هذا يجبر Dart على التحقق وقت البناء (compile time) أن هذا
الكلاس ينفّذ **كل** الدوال المعرَّفة في الواجهة أعلاه، وإلا سيرفض المشروع البناء بخطأ واضح — حماية
مبكرة من نسيان تنفيذ دالة.

```dart
  const OnboardingRepositoryImpl(this._local);
  final OnboardingLocalDataSource _local;

  @override
  Future<bool> hasSeenOnboarding() => _local.hasSeenOnboarding();

  @override
  Future<void> completeOnboarding() => _local.markOnboardingSeen();
```
- نفس مبدأ حقن الاعتماديات من قبل: يستقبل `OnboardingLocalDataSource` جاهزًا من الخارج.
- `@override`: يعلن صراحة "أنا أنفّذ دالة موجودة في الواجهة الأب"، ويجعل Dart يتحقق أن التوقيع
  (اسم الدالة، أنواع المدخلات والمخرجات) مطابق تمامًا.
- لاحظ الصيغة المختصرة `=> _local.hasSeenOnboarding();` بدل `{ return ...; }` — تُستخدم في Dart
  عندما تكون الدالة سطرًا واحدًا فقط، وهي مجرد "تمرير" (delegation) للدالة المكافئة في الـ
  DataSource. **هنا بالضبط تظهر فائدة الفصل بين الاسمين**: لو غيّرنا رأينا لاحقًا وأردنا أن
  "إكمال الترحيب" يحفظ أيضًا الوقت والتاريخ، نُعدّل هذا السطر فقط دون المساس بـ DataSource أو
  بأي شاشة تستخدم الـ Repository.

### 7.7) `lib/logic/onboarding/onboarding_provider.dart`

هذا أهم ملف في طبقة `logic/` لميزة الترحيب — هنا تعيش "الحالة" الفعلية التي تراقبها الشاشة.

```dart
final sharedPreferencesProvider = Provider<SharedPreferences>(
  (ref) => throw UnimplementedError('sharedPreferencesProvider must be overridden in main.dart'),
);
```
هذا **متعمد أن يفشل** لو استُخدم كما هو! `Provider` في Riverpod هو "صندوق" يحمل قيمة يمكن لأي
جزء من التطبيق طلبها عبر `ref.watch(...)`. لكن هنا وضعنا دالة تُطلق خطأ عمدًا، لأن القيمة الحقيقية
(نسخة SharedPreferences الفعلية) لا تكون جاهزة إلا بعد `await SharedPreferences.getInstance()`
في `main.dart` (سنراها في قسم 7.11) — وهذه عملية غير متزامنة يجب انتظارها **قبل** بدء التطبيق.
لذلك `main.dart` "يستبدل" (override) هذا الصندوق الفارغ بالقيمة الحقيقية بمجرد أن تصبح جاهزة.
لو نسي أحد المطورين هذا الاستبدال، سيحصل على خطأ واضح فورًا بدل تعطّل غامض لاحقًا.

```dart
final onboardingLocalDataSourceProvider = Provider<OnboardingLocalDataSource>(
  (ref) => OnboardingLocalDataSource(ref.watch(sharedPreferencesProvider)),
);

final onboardingRepositoryProvider = Provider<OnboardingRepository>(
  (ref) => OnboardingRepositoryImpl(ref.watch(onboardingLocalDataSourceProvider)),
);
```
هذه "سلسلة تجميع" (composition chain) — كل صندوق يبني على الذي قبله تلقائيًا:
`sharedPreferencesProvider` ← `onboardingLocalDataSourceProvider` ← `onboardingRepositoryProvider`.
`ref.watch(...)` تعني "أعطني قيمة هذا الصندوق الآن، وأعد بناء هذا الصندوق تلقائيًا لو تغيّرت
قيمته لاحقًا". لاحظ نوع الصندوق الأخير: `Provider<OnboardingRepository>` — أي أن أي كود يطلبه
يرى **الواجهة المجردة فقط** (`OnboardingRepository`) لا التنفيذ الحقيقي (`OnboardingRepositoryImpl`)
— نفس فكرة الفصل من قسم 7.5 و7.6 لكن الآن مطبّقة على مستوى نظام الحقن نفسه، وهذا بالضبط ما يسمح
للاختبارات لاحقًا باستبدال هذا الصندوق بـ Mock بسطر واحد (`overrideWithValue`).

```dart
final onboardingCompletedProvider =
    AsyncNotifierProvider<OnboardingCompletedController, bool>(
  OnboardingCompletedController.new,
);

class OnboardingCompletedController extends AsyncNotifier<bool> {
  @override
  Future<bool> build() => ref.read(onboardingRepositoryProvider).hasSeenOnboarding();

  Future<void> complete() async {
    await ref.read(onboardingRepositoryProvider).completeOnboarding();
    state = const AsyncData(true);
  }
}
```
هذا "قلب" حالة الترحيب. `AsyncNotifier<bool>` نوع خاص في Riverpod مصمَّم لحالة تُحمَّل بشكل غير
متزامن (من التخزين) ثم يمكن تعديلها لاحقًا:
- `build()`: تُستدعى **تلقائيًا مرة واحدة** أول ما يطلب أي كود هذا الصندوق — تقرأ القيمة الحالية
  من الـ Repository. أثناء انتظار النتيجة، أي كود يراقب هذا الصندوق يرى حالة "تحميل" تلقائيًا،
  وهذا بالضبط ما تستخدمه `SplashGate` (قسم 8) لعرض دائرة تحميل بينما تُقرأ القيمة.
- `complete()`: تُستدعى يدويًا عند ضغط المستخدم على "ابدأ الآن". أولًا تنتظر حفظ القيمة فعليًا في
  التخزين (`await ...completeOnboarding()`)، **ثم فقط** تُحدّث الحالة محليًا إلى
  `AsyncData(true)`. لماذا هذا الترتيب مهم؟ لو حدّثنا الحالة قبل التأكد من نجاح الحفظ، وفشل الحفظ
  فعليًا (مثلًا التخزين ممتلئ)، ستظن الشاشة أن كل شيء نجح بينما لم يُحفظ شيء فعليًا على الجهاز.
- `ref.read(...)` هنا وليس `ref.watch(...)`: `read` تعني "أعطني القيمة الآن فقط، لا أريد إعادة
  بناء هذه الدالة لو تغيّر الصندوق لاحقًا" — مناسبة داخل دالة تُستدعى مرة واحدة عند ضغطة، بعكس
  `watch` المناسبة داخل `build()` لأنها تحتاج مراقبة مستمرة.

```dart
final onboardingPageIndexProvider = StateProvider.autoDispose<int>((ref) => 0);
```
صندوق أبسط بكثير: مجرد رقم صحيح (أي شريحة معروضة الآن، يبدأ من 0). `StateProvider` مناسب لحالة
بسيطة لا تحتاج منطق `build()/complete()` معقّد — فقط قيمة تتغيّر مباشرة. `.autoDispose` تعني:
"احذف هذه الحالة من الذاكرة تلقائيًا بمجرد أن يغادر المستخدم شاشة الترحيب" — لأنها حالة **مؤقتة**
لا فائدة من إبقائها في الذاكرة بعد انتهاء الغرض منها (بعكس `onboardingCompletedProvider` الذي
يجب أن يبقى محفوظًا لأن الشاشات الأخرى، مثل `SplashGate`، قد تحتاج قراءته لاحقًا).

### 7.8) ودجات الترحيب — `lib/view/widgets/onboarding/`

كل ملف هنا **خاص بميزة الترحيب فقط** (بعكس `widgets/shared/` المشروح في قسم 8) — لهذا وُضعت هنا
لا في `widgets/shared/`، لأنه لا معنى لاستخدام "بطاقة محادثة الترحيب" في شاشة تسجيل الدخول مثلًا.

#### `feature_icon_row.dart`
```dart
class FeatureIconItem {
  const FeatureIconItem({required this.icon, required this.label});
  final IconData icon;
  final String label;
}
```
كلاس بسيط جدًا وظيفته فقط **حزم بيانات معًا** (أيقونة + نص) — لا يحتوي أي منطق أو واجهة، فقط
"حاوية بيانات". لماذا نحتاجه؟ لأن الشريحة صف من 3 عناصر متكررة الشكل (أيقونة دائرية + تسمية تحتها)
لكن بمحتوى مختلف في كل شريحة — بدل تكرار نفس كود الرسم 3 مرات يدويًا في كل شريحة، نمرر **قائمة**
من `FeatureIconItem` وندع الودجة ترسمها تلقائيًا.

```dart
class FeatureIconRow extends StatelessWidget {
  const FeatureIconRow({required this.items, super.key});
  final List<FeatureIconItem> items;

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final tint = isDark ? AppColors.primaryDark : AppColors.primary;

    return Row(
      mainAxisAlignment: MainAxisAlignment.spaceEvenly,
      children: items.map((item) => Column(...)).toList(),
    );
  }
}
```
- `StatelessWidget`: لا تملك "حالة" داخلية تتغيّر — تُعطى بيانات جاهزة (`items`) وترسمها فقط،
  ولا "تتذكر" شيئًا بين إعادات الرسم. مناسبة تمامًا هنا لأن هذه الودجة "عرض فقط" بلا تفاعل.
- `Theme.of(context).brightness == Brightness.dark`: يسأل نظام التشغيل/التطبيق "هل الوضع الحالي
  داكن أم فاتح؟" لاختيار درجة اللون المناسبة (`primaryDark` أخف قليلًا ليكون مقروءًا على خلفية
  داكنة، بعكس `primary` الداكن المخصص للخلفية الفاتحة).
- `items.map((item) => Column(...)).toList()`: يحوّل كل عنصر بيانات (`FeatureIconItem`) إلى ودجة
  `Column` (أيقونة فوق نص) — هذا نمط شائع جدًا في Flutter اسمه "map to widgets": حوّل قائمة بيانات
  إلى قائمة ودجات بقاعدة واحدة، بدل نسخ نفس الكود لكل عنصر يدويًا.
- `mainAxisAlignment: MainAxisAlignment.spaceEvenly`: يوزّع العناصر الثلاثة بمسافات متساوية عبر
  عرض الشاشة، مطابقًا لتوزيعها في التصميم المرجعي.

#### `onboarding_chat_preview.dart`
هذا أعقد ودجة في الترحيب لأنه يحاكي محادثة كاملة (الشريحة 3 في التصميم).

```dart
class ChatTurn {
  const ChatTurn({required this.text, required this.fromAssistant});
  final String text;
  final bool fromAssistant;
}
```
نفس فكرة "حاوية بيانات" كما في `FeatureIconItem`، لكن هنا `fromAssistant` (بدل مجرد نص وأيقونة)
يحدد **من قال هذه الجملة**: `true` = فقاعة خضراء على اليمين (المساعد)، `false` = فقاعة بيضاء على
اليسار (المستخدم) — تمامًا كما تُبنى شاشات المحادثات في تطبيقات مثل واتساب.

```dart
for (final turn in turns) ...[
  Align(
    alignment: turn.fromAssistant
        ? AlignmentDirectional.centerEnd
        : AlignmentDirectional.centerStart,
    child: Container(
      ...
      decoration: BoxDecoration(
        color: turn.fromAssistant
            ? tint
            : (isDark ? AppColors.backgroundDark : AppColors.backgroundLight),
        ...
      ),
      child: Text(turn.text, ...),
    ),
  ),
],
```
- `AlignmentDirectional.centerEnd` / `centerStart` وليس `centerRight`/`centerLeft`: هذا **فرق
  جوهري ومقصود**. `Directional` تعني "بداية/نهاية حسب اتجاه اللغة" — في العربية (RTL) تصبح
  `centerEnd` تلقائيًا هي اليسار، بينما في الإنجليزية (LTR) تصبح هي اليمين. لو استخدمنا
  `centerRight` الثابتة، ستظهر فقاعات المساعد على اليمين في الإنجليزية أيضًا وهو **عكس** ما يفعله
  أي تطبيق محادثات حقيقي مبني بشكل صحيح لدعم اللغتين.
- `for (final turn in turns) ...[...]`: هذا "for loop" داخل قائمة ودجات مباشرة (بدون
  `.map().toList()`) — أسلوب بديل بنفس النتيجة، استُخدم هنا بدل `.map()` لتجنّب تداخل الأقواس
  الكثيف عندما تكون الودجة الناتجة معقدة (تحتوي `Align` ثم `Container` ثم `Text` متداخلة).
- بعد حلقة الفقاعات، يوجد صف ثابت خارج الحلقة (لا يتكرر) يعرض دائرة ✓ خضراء + "تم الحفظ بنجاح" —
  هذا يمثل النتيجة النهائية للمحادثة، لذا وُضع بعد الحلقة لا داخلها.

**تعليق مهم مكتوب أعلى الكلاس:**
```dart
/// Static/decorative here — the real dialogue-confirmation flow ships in Phase 3 (voice).
```
هذا توثيق **مقصود** يوضّح أن هذه الودجة الآن **تعرض محادثة ثابتة مكتوبة مسبقًا فقط للتوضيح** —
وليست محادثة صوتية فعلية تفهم كلام المستخدم. الأسئلة والأجوبة هنا نصوص جاهزة من `app_ar.arb`
(`chatQ1`, `chatA1`...). المحادثة الحقيقية بالصوت تُبنى في المرحلة 3 من خارطة الطريق. هذا مثال
عملي على أهمية توثيق "ماذا يفعل الكود *الآن*" مقابل "ماذا سيفعل *لاحقًا*" حتى لا يظن أي مطور
مستقبلي (أو أنت بعد أشهر) أن هذه الميزة مكتملة فعليًا.

#### `onboarding_illustration.dart`
```dart
child: Image.asset(
  'assets/images/$assetName',
  fit: BoxFit.contain,
  errorBuilder: (context, error, stackTrace) => Container(
    decoration: BoxDecoration(color: tint.withValues(alpha: 0.12), shape: BoxShape.circle),
    child: Icon(fallbackIcon, size: 84, color: tint),
  ),
),
```
- `Image.asset('assets/images/$assetName')`: يحاول تحميل صورة حقيقية من مجلد `assets/images/`
  (الاسم يأتي كمعامل، مثل `onboarding_welcome.png`، حتى تُستخدم نفس الودجة لكل الشرائح الأربع
  بأسماء ملفات مختلفة فقط).
- `errorBuilder`: **هذا هو السبب الرئيسي لوجود هذا الملف بالذات**. بما أنك لم تزوّدنا بعد بملفات
  الرسومات الحقيقية (موثّق في README)، فإن `Image.asset` سيفشل في إيجاد الملف. بدل أن يتحطم
  التطبيق بخطأ لا يُفهم، يلتقط `errorBuilder` هذا الفشل تحديدًا ويعرض بديلًا (دائرة ملونة + أيقونة
  Material عامة) حتى تبقى الشاشة **قابلة للعرض والاختبار الكامل من اليوم الأول**، رغم أن الرسمة
  النهائية لم تصل بعد. بمجرد أن تضع الملف الحقيقي في المسار الصحيح، سيظهر تلقائيًا بدل البديل
  دون أي تعديل على الكود.

#### `page_indicator.dart`
```dart
AnimatedContainer(
  duration: const Duration(milliseconds: 200),
  ...
  decoration: BoxDecoration(
    color: isActive ? AppColors.indicatorActive : AppColors.indicatorInactive,
    shape: BoxShape.circle,
  ),
);
```
- `AnimatedContainer` وليس `Container` عادي: يجعل أي تغيّر في الخصائص (هنا اللون عند تغيّر
  الشريحة النشطة) **ينتقل بسلاسة** عبر `duration` المحددة بدل التبديل الفجائي — لمسة صغيرة لكنها
  تجعل التنقل بين الشرائح يبدو أكثر احترافية.
- `List.generate(count, (i) { ... })`: يبني تلقائيًا عددًا من النقاط مساويًا لعدد الشرائح
  (`count`)، بدل كتابة 4 نقاط يدويًا بشكل ثابت — لو أضفت شريحة خامسة لاحقًا، يكفي تغيير رقم واحد
  (`_pageCount` في الشاشة نفسها) وستتكيف النقاط تلقائيًا.

#### `step_badge.dart`
```dart
Text(
  number.toString().padLeft(2, '0'),
  ...
)
```
`number.toString()` يحوّل الرقم الصحيح (مثل `1`) لنص (`"1"`)، ثم `.padLeft(2, '0')` يضيف أصفارًا
لليسار حتى يصبح طوله حرفين (`"01"`) — مطابقًا تمامًا للشارات في التصميم المرجعي ("01", "02"...).

### 7.9) `lib/core/utils/app_exception.dart` و `result.dart` (استخدامهما داخل الترحيب)

ذكرنا `StorageException` سابقًا في قسم 7.4 دون شرح مصدرها — هذا مصدرها:

```dart
sealed class AppException implements Exception {
  const AppException(this.code, {this.internalDetail});
  final String code;
  final String? internalDetail;
}

class StorageException extends AppException {
  const StorageException({super.internalDetail}) : super('storage_unavailable');
}
```
- `sealed class`: يعني أن Dart يعرف **مسبقًا القائمة الكاملة** لكل الأنواع الفرعية الممكنة
  (`StorageException`, `ValidationException`, `UnexpectedException`...). الفائدة: لو استخدمت
  `switch` للتعامل مع كل الأنواع الممكنة ونسيت نوعًا واحدًا، Dart يرفض البناء ويخبرك بالضبط ماذا
  نسيت — هذا استخدمناه فعليًا في شاشتي الدخول والتسجيل (قسم 8) عند تحويل الخطأ لرسالة مفهومة.
- `code`: نص قصير مثل `'storage_unavailable'` **وليس** الرسالة النهائية التي يراها المستخدم —
  الغرض منه أن تقرر كل شاشة بنفسها كيف تترجم هذا الكود لجملة عربية أو إنجليزية مناسبة (عبر ملفات
  الترجمة من قسم 7.1)، بدل أن تُكتب الرسالة النهائية هنا في `core/` حيث لا معنى للغة محددة.
- `internalDetail`: تفاصيل تقنية خام (نص الخطأ الأصلي من النظام) **لغرض التسجيل فقط**
  (logs) — لا تُعرض للمستخدم أبدًا، لأن رسالة خطأ تقنية خام قد تكون مربكة أو حتى تكشف تفاصيل
  داخلية لا يجب أن يراها المستخدم العادي.

ملف `result.dart` (نمط `Result<T>` مع `Success`/`Failure`) **لم يُستخدم فعليًا بعد داخل ميزة
الترحيب تحديدًا** — لأن `OnboardingCompletedController` استخدم نمط `AsyncNotifier` المدمج في
Riverpod نفسه بدل بنائه يدويًا (Riverpod يمنحك هذا التمييز بين "تحميل/نجاح/خطأ" جاهزًا). ستراه
مستخدمًا صراحة في طبقة المصادقة (قسم 8) حيث الحاجة إليه أوضح.

### 7.10) `lib/view/screens/onboarding/onboarding_screen.dart`

هذا الملف الأكبر والأهم — الشاشة الفعلية التي يراها المستخدم. سنمر عليه بالكامل.

```dart
class OnboardingScreen extends ConsumerStatefulWidget {
```
`ConsumerStatefulWidget` وليس `StatelessWidget` العادي مثل الودجات الصغيرة السابقة، للسببين
معًا:
- `Stateful`: لأن هذه الشاشة **تحتاج تذكّر شيء بين إعادات الرسم** — تحديدًا `PageController`
  (المتحكم في التمرير بين الشرائح) الذي يجب أن يبقى **نفس الكائن** طوال حياة الشاشة، لا يُعاد
  إنشاؤه في كل مرة.
- `Consumer`: بادئة من Riverpod تعني أن هذه الودجة تستطيع استخدام `ref.watch(...)` و`ref.read(...)`
  لقراءة الصناديق (Providers) التي شرحناها في قسم 7.7.

```dart
class _OnboardingScreenState extends ConsumerState<OnboardingScreen> {
  final _pageController = PageController();
  static const _pageCount = 4;

  @override
  void dispose() {
    _pageController.dispose();
    super.dispose();
  }
```
- `PageController()`: الكائن المسؤول عن التحكم البرمجي بالتمرير (مثل الانتقال للشريحة التالية
  عبر كود، لا سحب إصبع فقط).
- `dispose()`: تُستدعى تلقائيًا عندما تُغلق الشاشة نهائيًا (المستخدم غادرها). **يجب** استدعاء
  `_pageController.dispose()` هنا يدويًا — لو نسينا هذا، يبقى الكائن في الذاكرة حتى بعد مغادرة
  الشاشة (تسريب ذاكرة). هذا بالضبط ما تكتشفه قاعدة `close_sinks`/`cancel_subscriptions` في
  `analysis_options.yaml` (قسم 4) لو نسيتها.

```dart
  Future<void> _complete() async {
    await ref.read(onboardingCompletedProvider.notifier).complete();
    if (mounted) context.go('/login');
  }
```
- `ref.read(onboardingCompletedProvider.notifier).complete()`: يستدعي الدالة `complete()` التي
  شرحناها في قسم 7.7 — تحفظ الحالة فعليًا ثم تُحدّث الصندوق.
- `if (mounted)`: فحص أمان مهم جدًا — بين لحظة استدعاء `await` ولحظة عودة النتيجة، قد يكون
  المستخدم غادر الشاشة بالفعل (مثلًا ضغط زر الرجوع بسرعة). لو حاولنا التنقل (`context.go`) على
  شاشة لم تعد موجودة، سيرمي Flutter خطأ. `mounted` يتحقق أن الشاشة ما زالت "حيّة" قبل استخدام
  `context` الخاص بها.

```dart
  void _next(int currentIndex) {
    if (currentIndex == _pageCount - 1) {
      _complete();
      return;
    }
    _pageController.nextPage(duration: const Duration(milliseconds: 300), curve: Curves.easeOut);
  }
```
منطق بسيط: لو كنا في آخر شريحة (index يبدأ من 0، فآخر شريحة هي `_pageCount - 1` أي 3)، استدعِ
`_complete()` بدل الانتقال لشريحة تالية غير موجودة. وإلا، حرّك `PageController` للشريحة التالية
بحركة سلسة (300 ملي ثانية، منحنى `easeOut` يعني "يبدأ سريعًا وينتهي ببطء" — إحساس طبيعي أكثر من
حركة بسرعة ثابتة).

```dart
final l10n = AppLocalizations.of(context)!;
final currentIndex = ref.watch(onboardingPageIndexProvider);
final isLast = currentIndex == _pageCount - 1;
final isDark = Theme.of(context).brightness == Brightness.dark;
final tint = isDark ? AppColors.primaryDark : AppColors.primary;
```
هذه أول 5 أسطر داخل `build()` — تُحضّر كل القيم التي سيحتاجها بقية الكود:
- `AppLocalizations.of(context)!`: يجلب كائن الترجمات الحالي (الذي شرحناه في 7.1). علامة `!`
  تعني "أنا متأكد أن هذا ليس `null`" — آمن هنا لأننا سجّلنا `localizationsDelegates` في
  `main.dart` (قسم 7.11)، فلن يكون `null` أبدًا في تطبيق مُعدّ بشكل صحيح.
- `ref.watch(onboardingPageIndexProvider)`: **هذا هو ما يجعل الشاشة "تعيد الرسم" تلقائيًا** كلما
  تغيّرت الشريحة الحالية — أي كود يستخدم `ref.watch` يُعاد تشغيله تلقائيًا كلما تغيّرت القيمة
  التي يراقبها، بدون أي `setState()` يدوي.

```dart
final pages = [
  _OnboardingSlide(badgeNumber: 1, ...),
  _OnboardingSlide(badgeNumber: 2, ...),
  _OnboardingSlide(badgeNumber: 3, ...),
  _OnboardingSlide(badgeNumber: 4, ...),
];
```
قائمة من 4 عناصر، كل عنصر هو استدعاء لكلاس `_OnboardingSlide` الخاص (المعرَّف أسفل نفس الملف —
سنشرحه بعد قليل) بمحتوى مختلف لكل شريحة: رقم الشارة، الرسمة، العنوان، النص الفرعي، والودجة
الإضافية (صف أيقونات أو محادثة). لاحظ أن **الشريحة الوحيدة المختلفة بنيويًا** هي الأولى — تملك
`brandLine` (اسم التطبيق) و`subtitleSpans` (نص متعدد الألوان) بدل `subtitle` عادي، لأنها الوحيدة
التي تعرض اسم العلامة التجارية والكلمة الذهبية المميزة.

```dart
return Scaffold(
  body: SafeArea(
    child: Column(
      children: [
        Expanded(
          child: PageView(
            controller: _pageController,
            onPageChanged: (i) => ref.read(onboardingPageIndexProvider.notifier).state = i,
            children: pages,
          ),
        ),
        PageIndicator(count: _pageCount, currentIndex: currentIndex),
        ...
```
- `SafeArea`: يمنع رسم المحتوى خلف نتوءات الشاشة (الكاميرا الأمامية، شريط الحالة) — مهم جدًا في
  أي شاشة كاملة الارتفاع.
- `Expanded`: يجبر `PageView` على أخذ **كل المساحة المتبقية** بعد حجز المساحة اللازمة للعناصر
  الثابتة تحته (المؤشر والزر) — بدونها سيحصل خطأ "حجم غير محدود" لأن `PageView` بطبيعته يحاول
  أخذ مساحة غير منتهية.
- `onPageChanged: (i) => ref.read(onboardingPageIndexProvider.notifier).state = i`: كلما سحب
  المستخدم إصبعه لشريحة جديدة (وليس فقط عبر ضغط الزر)، يُحدَّث الصندوق `onboardingPageIndexProvider`
  مباشرة — هذا يضمن أن المؤشر السفلي (`PageIndicator`) يبقى متزامنًا مع الشريحة المعروضة فعليًا
  بغض النظر عن طريقة التنقل (سحب أو زر).

```dart
PrimaryButton(
  label: isLast ? l10n.onboardingStart : l10n.onboardingNext,
  onPressed: () => _next(currentIndex),
  leadingIcon: isLast ? null : Icons.arrow_back_ios_new_rounded,
  backgroundColor: isLast ? AppColors.accent : null,
  foregroundColor: isLast ? AppColors.primary : null,
),
```
كل خاصية هنا تتغيّر حسب `isLast` — هذا يطبّق ما رأيناه في التصميم المرجعي مباشرة: النص يتغيّر
("التالي" ← "ابدأ الآن")، السهم يختفي في آخر شريحة، ولون الزر يتحول من الأخضر (`null` يعني
"استخدم اللون الافتراضي من الثيم") إلى الذهبي `AppColors.accent`. هذا **مثال حي على "ودجة واحدة،
تصرفات متعددة حسب الحالة"** بدل بناء ودجتين منفصلتين (زر عادي + زر أخير) بكود مكرر.

```dart
class _OnboardingSlide extends StatelessWidget {
```
لاحظ الشرطة السفلية `_` في بداية الاسم — هذا يعني أن هذا الكلاس **خاص بهذا الملف فقط** (private
to library)، لا يمكن لأي ملف آخر استيراده حتى لو أراد. هذا مقصود: `_OnboardingSlide` قالب داخلي
لتنظيم كود هذا الملف فقط، وليس مكوّنًا عامًا يُفترض استخدامه في مكان آخر (بعكس الودجات في قسم
7.8 الموجودة في ملفات منفصلة لأنها فعلًا قابلة لإعادة الاستخدام عبر أكثر من مكان).

```dart
const _OnboardingSlide({
  required this.badgeNumber,
  required this.illustration,
  required this.title,
  this.brandLine,
  this.subtitle,
  this.subtitleSpans,
  this.extra,
}) : assert(
        subtitle != null || subtitleSpans != null,
        'Provide either subtitle or subtitleSpans',
      );
```
- المعاملات `required` (مثل `badgeNumber`, `illustration`, `title`) **يجب** تمريرها دائمًا —
  Dart يرفض البناء إن نسيتها. أما `this.brandLine`, `this.subtitle`... بدون `required` فهي
  اختيارية (تقبل `null`).
- `assert(...)`: فحص أمان **وقت التطوير فقط** (يُزال تلقائيًا في نسخة الإصدار النهائية لتحسين
  الأداء) — يتأكد أن أحد الاثنين (`subtitle` أو `subtitleSpans`) مُمرَّر دائمًا، وإلا سيتحطم
  التطبيق فورًا بخطأ واضح **وقت التطوير** بدل عرض شاشة فارغة بصمت. هذا نمط دفاعي جيد لأي ودجة
  تقبل أكثر من "طريقة" لتمرير نفس نوع المعلومة.

```dart
Text.rich(
  TextSpan(
    children: subtitleSpans ?? [TextSpan(text: subtitle)],
    style: AppTextStyles.body(context).copyWith(color: secondaryColor),
  ),
  textAlign: TextAlign.center,
),
```
هنا يظهر السبب الحقيقي وراء تقسيم نص الترجمة في قسم 7.1: `subtitleSpans ?? [TextSpan(text: subtitle)]`
تعني "لو مُرِّرت قائمة `TextSpan` جاهزة (الشريحة 1 فقط، بألوانها المختلفة)، استخدمها كما هي؛ وإلا
حوّل النص العادي (`subtitle`) لعنصر `TextSpan` واحد بسيط". هذا يسمح لنفس الودجة (`_OnboardingSlide`)
بعرض نص عادي بلون واحد **أو** نص متعدد الألوان، حسب ما تحتاجه كل شريحة، دون تكرار كود بناء
`_OnboardingSlide` نفسه لكل حالة.

### 7.11) الملفات التي "تُشغّل" شاشة الترحيب فعليًا

هذه الملفات ليست جزءًا من مجلد `onboarding/` نفسه، لكن بدونها الشاشة لن تظهر أبدًا — لذا لا بد
من فهمها ضمن نفس الرحلة.

#### `lib/view/screens/splash/splash_gate.dart`
```dart
class SplashGate extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final completedAsync = ref.watch(onboardingCompletedProvider);

    return completedAsync.when(
      data: (hasSeenOnboarding) {
        WidgetsBinding.instance.addPostFrameCallback((_) {
          context.go(hasSeenOnboarding ? '/login' : '/onboarding');
        });
        return const _SplashBody();
      },
      loading: () => const _SplashBody(),
      error: (_, __) {
        WidgetsBinding.instance.addPostFrameCallback((_) => context.go('/onboarding'));
        return const _SplashBody();
      },
    );
  }
}
```
هذه الشاشة **لا تُرى فعليًا لأكثر من لحظة** — وظيفتها فقط تقرر "هل أذهب للترحيب أم لتسجيل
الدخول؟" بناءً على القيمة التي شرحناها في `onboardingCompletedProvider` (قسم 7.7).
- `.when(data: ..., loading: ..., error: ...)`: هذه الطريقة القياسية في Riverpod للتعامل مع أي
  قيمة "غير متزامنة" (`AsyncValue`) — تجبرك على معالجة **الحالات الثلاث الممكنة كلها**: نجحت
  القراءة (`data`)، ما زالت جارية (`loading`)، أو فشلت (`error`). لا يمكن نسيان حالة منها لأن
  Dart يطلب الثلاثة معًا.
- `addPostFrameCallback`: **تفصيل تقني مهم** — لا يمكن استدعاء `context.go(...)` (تنقّل) أثناء
  عملية "الرسم" (build) نفسها، لأن هذا يسبب تعارضًا داخل Flutter. هذه الدالة تقول: "نفّذ هذا
  التنقل بعد انتهاء رسم هذا الإطار مباشرة" — حل قياسي لمشكلة "التنقل من داخل `build()`".
- في حالة `error`، بدل ترك المستخدم عالقًا في شاشة فارغة، نفترض بأمان أنه لم يرَ الترحيب بعد
  ونرسله هناك — قرار "تعطّل آمن" (fail-safe) مقصود بدل حجب المستخدم عن استخدام التطبيق كليًا.

#### `lib/core/routes/app_router.dart`
```dart
final appRouterProvider = Provider<GoRouter>((ref) {
  return GoRouter(
    initialLocation: '/',
    routes: [
      GoRoute(path: '/', builder: (_, __) => const SplashGate()),
      GoRoute(path: '/onboarding', builder: (_, __) => const OnboardingScreen()),
      GoRoute(path: '/login', builder: (_, __) => const LoginScreen()),
      GoRoute(path: '/signup', builder: (_, __) => const SignupScreen()),
      GoRoute(path: '/home', builder: (_, __) => const HomeScreenPlaceholder()),
    ],
  );
});
```
"خريطة الطريق" الكاملة للتطبيق في مكان واحد. عندما تكتب `context.go('/onboarding')` في أي مكان
بالتطبيق (كما رأينا في `onboarding_screen.dart`)، هذا الملف هو من يقرر "إذن اعرض `OnboardingScreen`".
لماذا هذا أفضل من التنقل المباشر (`Navigator.push(MaterialPageRoute(...))`)؟ لأن كل المسارات
مرئية في مكان واحد ("خريطة" واضحة لكل شاشات التطبيق)، ويسهل لاحقًا إضافة منطق حماية (مثل "امنع
الوصول لـ `/home` قبل تسجيل الدخول") في نقطة مركزية واحدة بدل نثره عبر كل الشاشات.

#### `lib/main.dart`
```dart
Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  final prefs = await SharedPreferences.getInstance();

  runApp(
    ProviderScope(
      overrides: [sharedPreferencesProvider.overrideWithValue(prefs)],
      child: const App(),
    ),
  );
}
```
هذه أول 10 أسطر تُنفَّذ فعليًا عند فتح التطبيق:
- `WidgetsFlutterBinding.ensureInitialized()`: تهيئة إلزامية قبل استخدام أي شيء يحتاج التواصل
  مع نظام التشغيل (كـ SharedPreferences) قبل استدعاء `runApp`.
- `await SharedPreferences.getInstance()`: هنا بالضبط تُحل "المشكلة" التي زرعناها عمدًا في قسم
  7.7 — ننتظر السماح الفعلي بالوصول للتخزين، **ثم** نمرره لصندوق `sharedPreferencesProvider عبر:
- `ProviderScope(overrides: [sharedPreferencesProvider.overrideWithValue(prefs)], ...)`:
  `ProviderScope` هو "الحاوية" الجذرية التي يجب أن تُحيط بكامل تطبيق Riverpod (بدونها، لن يعمل
  أي `ref.watch`/`ref.read` في أي مكان). `overrides` هنا يستبدل الصندوق الذي كان "يرمي خطأ عمدًا"
  بالقيمة الحقيقية الجاهزة الآن.

```dart
class App extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final router = ref.watch(appRouterProvider);
    return MaterialApp.router(
      ...
      theme: AppTheme.light,
      darkTheme: AppTheme.dark,
      themeMode: ThemeMode.system,
      locale: const Locale('ar'),
      supportedLocales: const [Locale('ar'), Locale('en')],
      localizationsDelegates: AppLocalizations.localizationsDelegates,
      routerConfig: router,
    );
  }
}
```
- `MaterialApp.router(...)` وليس `MaterialApp(home: ...)` العادي: النسخة المخصصة للعمل مع
  `go_router` (نظام التنقل من قسم أعلاه) بدل نظام التنقل الافتراضي في Flutter.
  `routerConfig: router` هو ما يربط كل خريطة المسارات الفعلية بالتطبيق.
- `themeMode: ThemeMode.system`: يجعل التطبيق يتبع تلقائيًا إعداد "الوضع الداكن" في نظام تشغيل
  المستخدم، دون الحاجة لزر تبديل يدوي (رغم أن هذا لا يمنع إضافته لاحقًا).
- `locale: const Locale('ar')`: اللغة الافتراضية عربي عند أول تشغيل — هذا ما يجعل كل الأمثلة في
  هذا الدليل تظهر بالعربي افتراضيًا ما لم يُغيّر المستخدم اللغة يدويًا (ميزة تبديل اللغة الفعلية
  لم تُربط بعد، موثّقة كـ TODO في شاشة الدخول).

### 7.12) اختبارات الترحيب

#### `test/unit/onboarding/onboarding_controller_test.dart`
يختبر `OnboardingCompletedController` (قسم 7.7) **بمعزل تام** عن أي شاشة:
```dart
class MockOnboardingRepository extends Mock implements OnboardingRepository {}
...
container = ProviderContainer(
  overrides: [onboardingRepositoryProvider.overrideWithValue(repository)],
);
```
`ProviderContainer` هو النسخة "بدون واجهة" من `ProviderScope` — يسمح باختبار صناديق Riverpod
مباشرة دون رسم أي `Widget`. نفس فكرة `overrides` من `main.dart` بالضبط، لكن هنا نستبدل
الـ Repository الحقيقي بـ `MockOnboardingRepository` الوهمي، مستفيدين تمامًا من فصل الواجهة عن
التنفيذ الذي شرحناه في قسم 7.5.

```dart
test('complete() persists the flag and updates state to true', () async {
  when(() => repository.hasSeenOnboarding()).thenAnswer((_) async => false);
  when(() => repository.completeOnboarding()).thenAnswer((_) async {});

  await container.read(onboardingCompletedProvider.future);
  await container.read(onboardingCompletedProvider.notifier).complete();

  verify(() => repository.completeOnboarding()).called(1);
  expect(container.read(onboardingCompletedProvider).value, isTrue);
});
```
- `when(...).thenAnswer(...)`: "لو استُدعيت هذه الدالة، تظاهر بأنها أرجعت هذه القيمة" — هكذا
  نتحكم بسلوك الـ Mock دون تخزين حقيقي فعلي.
- `verify(...).called(1)`: تأكيد أن `completeOnboarding()` **استُدعيت فعليًا مرة واحدة بالضبط** —
  هذا يثبت أن `complete()` في الكنترولر فعلًا تتواصل مع الـ Repository، لا فقط تُحدّث الحالة
  محليًا بدون حفظ حقيقي (بالضبط الخطأ الذي حذّرنا منه في قسم 7.7).

#### `test/widget/onboarding/onboarding_screen_test.dart`
يختبر الشاشة **كما يراها المستخدم فعليًا**:
```dart
await tester.pumpWidget(_wrap(repository));
await tester.pumpAndSettle();
...
await tester.tap(find.text(l10n.onboardingNext));
await tester.pumpAndSettle();
expect(find.text(l10n.onboardingTitle2), findsOneWidget);
```
- `pumpWidget`: يرسم الشاشة فعليًا داخل بيئة اختبار محاكاة.
- `pumpAndSettle()`: ينتظر انتهاء كل الحركات والانتقالات (مثل حركة `PageView` بمدتها 300 ملي
  ثانية من قسم 7.10) قبل متابعة الاختبار — بدونها قد يفحص الاختبار حالة "منتصف الحركة" غير
  المستقرة.
- `tester.tap(find.text(...))`: يحاكي ضغطة مستخدم حقيقية على نص الزر.
- الاختبار الأخير في الملف يتحقق أن ضغط "ابدأ الآن" في آخر شريحة يستدعي فعليًا
  `completeOnboarding()` **وينقل** المستخدم لشاشة `/login` — تغطية كاملة للسيناريو من أول شريحة
  حتى الخروج من الترحيب بالكامل.

---

<a name="8"></a>
## 8) شرح باقي الشاشات والملفات

الترحيب أُخذ سطرًا سطرًا لأنه أول ميزة، وكل الأنماط (Pattern) المستخدمة فيه **تتكرر بنفس المنطق**
في الدخول والتسجيل. لذلك هنا سنشرح **الفروقات والإضافات الجديدة فقط** لكل ملف، دون تكرار شرح
مفاهيم سبق شرحها بالكامل أعلاه (مثل `Provider`, `AsyncNotifier`, `Repository`).

### 8.1) ودجات مشتركة — `lib/view/widgets/shared/`

هذه المرة الودجات هنا **فعلًا** مشتركة (بعكس Onboarding-specific)، تُستخدم في أكثر من ميزة:

- **`primary_button.dart`**: نفس الزر الأساسي من الترحيب، لكن طُوِّر لاحقًا ليقبل `leadingIcon`
  (أيقونة قبل النص، مثل سهم الترحيب أو أيقونة تسجيل الدخول) و`backgroundColor`/`foregroundColor`
  اختياريين (لدعم الزر الذهبي في آخر شريحة). هذا مثال على **تطوير ودجة موجودة بدل إنشاء نسخة
  جديدة** كلما احتجنا تنويعًا بسيطًا عليها.
- **`app_logo.dart`**: نفس فكرة `OnboardingIllustration` بالضبط (صورة حقيقية + بديل عند الفشل)،
  لكن مخصص للشعار فقط، وبديله عبارة عن حرف "د" داخل دائرة خضراء بدل أيقونة Material — لأن الشعار
  عنصر هوية بصرية له طبيعة مختلفة عن رسمة توضيحية عادية.
- **`app_text_field.dart`**: حقل إدخال نصي موحّد الشكل (حدود دائرية، لون تعبئة، أيقونة بداية/نهاية
  اختيارية) تستخدمه كل حقول شاشتي الدخول والتسجيل — بدل تكرار `TextFormField` بكل خصائصه في كل
  حقل من الحقول العشرة تقريبًا الموجودة في شاشة التسجيل وحدها.

### 8.2) ودجات خاصة بالمصادقة — `lib/view/widgets/auth/`

- **`social_login_button.dart`**: زر بيضاوي (Pill) بحد خارجي فقط (`OutlinedButton`) لأزرار "متابعة
  عبر Google/Apple" — يحتوي `enum SocialProvider { google, apple }` بدل إنشاء ملفين منفصلين
  متطابقين تقريبًا، ويحمل تعليقًا صريحًا يوضّح أن الأيقونتين الحاليتين عامتان مؤقتًا ريثما تُضاف
  الشعارات الرسمية (موثّق أيضًا في README).
- **`auth_stepper.dart`**: المؤشر الأفقي بـ4 خطوات في أعلى شاشة التسجيل. يستقبل قائمة `AuthStep`
  (تسمية + أيقونة + حالة: `done`/`current`/`upcoming`) ويرسمها تلقائيًا مع خطوط رابطة بينها —
  نفس فلسفة `FeatureIconRow` من الترحيب (بيانات مُمرَّرة كقائمة، الودجة تتكفل بالرسم) لكن بمنطق
  ألوان أكثر تعقيدًا حسب حالة كل خطوة.

### 8.3) طبقة بيانات ومنطق المصادقة

- **`lib/data/repositories/auth/auth_repository.dart`**: نفس نمط `OnboardingRepository`
  (واجهة مجردة فقط) لكن بدوال أكثر: `signInWithEmail`, `signUpWithEmail`, `signInWithGoogle`,
  `signOut`, وبث مستمر (`Stream<bool> authStateChanges`) لحالة تسجيل الدخول.
- **`auth_repository_impl.dart`**: التنفيذ الحقيقي عبر `firebase_auth` و`google_sign_in`. الفرق
  الجوهري عن `OnboardingRepositoryImpl` هو دالة خاصة `_mapAuthErrorCode(String code)` تحوّل
  أكواد خطأ Firebase الخام (مثل `'wrong-password'`) لمفاتيح ترجمة موحّدة (`'invalidCredentials'`)
  **قبل** رميها كـ `ValidationException` — بهذا، طبقة `view/` لا ترى أبدًا مصطلح Firebase الخام،
  فقط كودًا موحدًا تترجمه بنفسها. هذا يعني أيضًا أنه لو استبدلنا Firebase بخدمة أخرى مستقبلًا،
  الشاشات لن تحتاج أي تعديل — فقط هذا الملف الواحد يتغيّر.
- **`lib/logic/auth/auth_provider.dart`**: يحتوي `LoginController` و`SignupController`
  (كلاهما `AutoDisposeAsyncNotifier<void>`) — الفرق عن `OnboardingCompletedController` أن هذين
  لا "يبنيان" قيمة أولية (`build()` هنا فارغة تقريبًا `async {}`)، بل ينتظران فعل المستخدم
  (`submit(...)`) فقط. `AsyncValue.guard(() => ...)` طريقة اختصار في Riverpod تلتقط أي خطأ يُرمى
  من الدالة الممرَّرة وتحوّله تلقائيًا لحالة `AsyncError` — بدل كتابة `try/catch` يدويًا في كل
  دالة submit.

### 8.4) شاشتا الدخول والتسجيل

كلا الملفين (`login_screen.dart`, `signup_screen.dart`) يتبعان نفس هيكل `OnboardingScreen`
تمامًا: `ConsumerStatefulWidget` + `GlobalKey<FormState>` للتحقق من صحة الحقول + دالة `_submit()`
تستدعي الـ Controller ثم تتحقق من نجاح العملية قبل التنقل. أهم إضافة جديدة عنهما عن الترحيب:

```dart
String? _errorMessage(AppLocalizations l10n, Object? error) {
  if (error == null) return null;
  final code = (error as dynamic).code as String;
  return switch (code) {
    'invalidEmail' => l10n.invalidEmail,
    'invalidCredentials' => l10n.invalidCredentials,
    ...
    _ => l10n.unexpectedError,
  };
}
```
هذه الدالة هي **حلقة الوصل الأخيرة** في سلسلة معالجة الأخطاء بالكامل: بدأت بـ `AppException` في
`core/utils/` (قسم 7.9)، مرّت بترجمة Firebase في `auth_repository_impl.dart` (أعلاه)، وتنتهي هنا
بتحويل الكود النهائي لنص عربي/إنجليزي فعلي عبر `switch` على القيم — وبما أن `AppException`
`sealed class`، فلو أُضيف نوع خطأ جديد لاحقًا ونُسي إضافته هنا، لن ينكسر شيء فورًا (لأن `switch`
هنا على `String` لا على النوع نفسه) لكنه سيعرض `unexpectedError` الافتراضي بأمان بدل التحطم.

### 8.5) `lib/view/screens/home/home_screen_placeholder.dart`

أبسط ملف في المشروع بالكامل — شاشة فارغة تعرض نصًا واحدًا فقط:
```dart
class HomeScreenPlaceholder extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Home — Phase 1', ...)),
    );
  }
}
```
وظيفته الوحيدة إثبات أن التنقل (الترحيب ← الدخول/التسجيل ← الرئيسية) يعمل من طرف لطرف **قبل**
بناء الشاشة الرئيسية الحقيقية الكبيرة (التي ستأتي في مرحلة لاحقة من خارطة الطريق). التعليق أعلى
الكلاس (`// TODO(flutter-architect): replace with the real Home screen...`) يوثّق صراحة أن هذا
مؤقت ومصيره الاستبدال.

---

<a name="9"></a>
## 9) خلاصة: كيف تتحرك البيانات من الضغطة إلى الشاشة

لتثبيت كل ما سبق في صورة واحدة، إليك رحلة كاملة لحدث واحد بسيط: **المستخدم يضغط "ابدأ الآن" في
آخر شريحة ترحيب**.

```
1. المستخدم يضغط الزر
   └─▶ view/screens/onboarding/onboarding_screen.dart → _complete() تُستدعى

2. _complete() تطلب من طبقة المنطق تنفيذ الفعل
   └─▶ logic/onboarding/onboarding_provider.dart
       → ref.read(onboardingCompletedProvider.notifier).complete()

3. الكنترولر يطلب من المستودع تنفيذ "معنى العمل"
   └─▶ data/repositories/onboarding/onboarding_repository.dart (الواجهة)
       → data/repositories/onboarding/onboarding_repository_impl.dart (التنفيذ)
       → completeOnboarding()

4. المستودع يفوّض الحفظ الفعلي لمصدر البيانات
   └─▶ data/local/onboarding/onboarding_local_datasource.dart
       → markOnboardingSeen() → SharedPreferences.setBool(...)

5. الحفظ ينجح فعليًا على قرص الجهاز، والنتيجة تصعد للأعلى خطوة خطوة

6. الكنترولر يُحدّث حالته الداخلية
   └─▶ state = const AsyncData(true)

7. أي شاشة تراقب onboardingCompletedProvider تُعاد بناؤها تلقائيًا
   └─▶ (لا حاجة فعلية هنا لأن الشاشة ستُغادَر، لكن هذا ما يحصل نظريًا)

8. الشاشة تنتقل فعليًا
   └─▶ context.go('/login')
       → core/routes/app_router.dart يقرر: هذا المسار = LoginScreen
       → view/screens/auth/login_screen.dart تُرسم على الشاشة
```

**هذا التدرج بالذات (شاشة ← منطق ← مستودع ← مصدر بيانات) هو نفسه الذي سيتكرر لاحقًا لكل ميزة
قادمة** — تسجيل دين جديد، التقارير، حتى محرك الصوت في المرحلة 3. بمجرد أن تفهم هذه الرحلة
الكاملة لحدث واحد بسيط، ستفهم بنية أي ميزة جديدة تُضاف للمشروع مستقبلًا لأنها كلها مبنية على نفس
القاعدة بالضبط.
